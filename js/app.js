// Put this anywhere; normal scope rules will apply
let testModule = require('./module'); // import module.js

window.addEventListener('load', function () {
    console.log('ready to rock');

    // let housePic = document.querySelector('#house-pic');
    
    // let letters = document.querySelectorAll('p.clickable-letter');
    
    // // When the first letter gets clicked, run the function that's being passed
    // // as the second parameter.
    // letters[0].addEventListener('click', function () {
    //   housePic.src = '/house-1.png';
    // });
    
    // letters[1].addEventListener('click', function () {
    //   housePic.src = '/house-2.png';
    // });
});